const http = require("http");
const express =require ("express");
const app=express();


app.get('/' , (req,res)=>{
    return res.send("home page");
});
function myhandler(req,res){

}
const myserver = http.createServer(app);
myserver.listen(8000,()=>console;e.log("server started!"));